// Canonical icon choices shared across screens (CONVENTIONS.md §10.4) — the
// single source of truth so per-screen jobs don't each invent their own
// glyph for the same concept.
import { Sparkles, DraftingCompass } from "lucide-react";

export const IDEATE_ICON = Sparkles;
export const ARCHITECT_ICON = DraftingCompass;
